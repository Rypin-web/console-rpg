export type Spec_Stats = {
    'Воин': {
        hp: number, def: number, stats: {
            strength: number, agility: number, luck: number
        }
    },
    'Лучник': {
        hp: number, def: number, stats: {
            strength: number, agility: number, luck: number
        }
    }
}